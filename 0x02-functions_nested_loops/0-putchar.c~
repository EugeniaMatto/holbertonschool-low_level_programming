#include <stdio.h>
/**
 * main - asdas
 *
 * Return: always 0
 */
int main(void)
{
int i;
char[] string;
string = "_putchar";
for(i = 0; i < strlen(string); i++)
{
putchar(string[i]);
}
return (0);
}

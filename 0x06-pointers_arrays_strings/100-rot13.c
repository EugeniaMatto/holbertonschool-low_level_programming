#include "main.h"
/**
 * rot13 - codifica en rot13
 * @a: cadena a codificar
 */
char *rot13(char *a)
{
int i = 0;
      while (a[i] != '\0')
      {
	if (a[i] )
	
	      i++;
      }
        return (a);
}

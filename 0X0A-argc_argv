#include <stdio.h>
/**
 * main - das
 * @argc - cont argumentos
 */
int main(int argc, char *argv[])
{
int i = 0;
for (i = 0; i <= argc; i++)
{
if (i == 0)
printf("%d\n", argc - 1);
if (*argv[i] == '\0')
printf("\n");
return (0);
}
